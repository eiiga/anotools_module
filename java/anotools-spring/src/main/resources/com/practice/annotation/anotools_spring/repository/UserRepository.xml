<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
  namespace="com.practice.annotation.anotools_spring.repository.UserRepository">

  <select id="findAll" resultType="com.practice.annotation.anotools_spring.entity.UserEntity">
    SELECT * FROM M_USER_INFO
  </select>

  <select id="findOne" resultType="com.practice.annotation.anotools_spring.entity.UserEntity">
    SELECT * FROM M_USER_INFO WHERE id = #{id}
  </select>

  <update id="updateOne" parameterType="com.practice.annotation.anotools_spring.dto.UserDTO">
    UPDATE M_USER_INFO
    SET
    name = #{name},
    mailAddress = #{mailAddress}
    WHERE id = #{id}
  </update>

  <insert id="insertOne" parameterType="com.practice.annotation.anotools_spring.dto.UserDTO">
    <!-- idは自動採番 -->
    INSERT INTO M_USER_INFO(name, mailAddress)
    VALUES(#{name}, #{mailAddress})
  </insert>

</mapper>
